import {StyleSheet} from "react-native";

export const addWorkStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    view: {
        width: '100%',
        padding: 15,
        alignItems: 'center'
    }
})